version: "3.8"

services:
  fiscal-service:
    build:
      context: .
      dockerfile: Dockerfile
    image: fiscal-service:latest
    container_name: fiscal-service
    ports:
      - "${SERVICE_PORT:-5555}:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
      - ApiKey=${API_KEY}
      - FISCAL__Ambiente=${FISCAL_AMBIENTE:-Homologacao}
      - FISCAL__SalvarXmls=true
      - FISCAL__DiretorioXmls=/app/xmls
      - FISCAL__DiretorioSchemas=/app/schemas
      - FISCAL__NFSe__Habilitado=${FISCAL_NFSE_HABILITADO:-true}
      - FISCAL__NFSe__VersaoDps=${FISCAL_NFSE_VERSAO:-Ve101}
      - FISCAL__NFSe__DiretorioSchemas=/app/schemas/nfse
      - FISCAL__DiretorioCertificados=/app/certificados
      - FISCAL__TimeoutWs=${FISCAL_TIMEOUT_WS:-30}
      - Fiscal__Ibpt__Habilitado=${FISCAL_IBPT_HABILITADO:-true}
      - Fiscal__Ibpt__Token=${FISCAL_IBPT_TOKEN:-}
      - Fiscal__Ibpt__ArquivoTabela=${FISCAL_IBPT_ARQUIVO_TABELA:-/app/ibpt/TabelaIBPTax.csv}
      - Fiscal__Ibpt__UfTabela=${FISCAL_IBPT_UF_TABELA:-}
      - DataProtection__Provider=Database
      - Database__ConnectionString=Host=db;Port=5432;Database=fiscal_db;Username=fiscal_user;Password=${DB_PASSWORD}
    volumes:
      - fiscal_xmls:/app/xmls
      - fiscal_certs:/app/certificados
      - fiscal_logs:/app/logs
      - fiscal_ibpt:/app/ibpt
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  db:
    image: postgres:16-alpine
    container_name: fiscal-db
    environment:
      - POSTGRES_DB=fiscal_db
      - POSTGRES_USER=fiscal_user
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    volumes:
      - fiscal_pgdata:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fiscal_user -d fiscal_db"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  fiscal_xmls:
    driver: local
  fiscal_certs:
    driver: local
  fiscal_logs:
    driver: local
  fiscal_ibpt:
    driver: local
  fiscal_pgdata:
    driver: local
